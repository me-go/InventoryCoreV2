@model IEnumerable<InventoryCoreVisualStudio.Models.Caliber>

@{
    ViewData["Title"] = "Caliber List";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a class="btn btn-success btn-sm" asp-action="Create">Create New</a>
</p>
@*<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DecimalSize)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MetricSize)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DecimalSize)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MetricSize)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@
<div>
@foreach (var item in Model)
{
    <div class="col-md-4">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h3 class="panel-title">@item.Name</h3>
            </div>
            <div class="panel-body">
                <p>Decimal Size: @item.DecimalSize</p>
                <p>Metric Size: @item.MetricSize</p>
            </div>
            <div class="panel-footer">
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </div>
        </div>
    </div>
}
</div>